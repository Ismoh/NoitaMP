this is a hax... 
- Only loads this for coalmine (no way without modifying code to create these for other biomes)
- The ramp pixels are hard coded to be removed and only happen in coalmines

If these features are needed elsewhere then this can be expanded


